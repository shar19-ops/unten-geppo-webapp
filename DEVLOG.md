# 開発ログ

## 2026-07-20 プロジェクト開始・モック作成・データ層実装開始

- 要件整理: 社有車・私有車の運転記録(出庫時メーター、行先、アルコールチェック、運転者、給油量)をiPhoneで入力し、PCで月報として確認・印刷・Excel出力するアプリ。DBサーバーが無いため、v1はlocalStorage中心+JSON手動エクスポート/インポートで複数端末の運用に対応する方針とした(将来クラウドDB化する際に備え、`storage.js`経由でのみデータへ触れる設計にしている)。
- 参考にした既存アプリ: `genka-webapp`(原価計算書アプリ)。フレームワークなし・no-buildの方針、GitHub Pages + PWAの配布方法、localStorage + JSON export/importでの複数人運用パターンをそのまま踏襲。
- サンプル雛形 `運転月報.xlsx` をExcelのXMLレベルで解析し、G列(走行距離)の数式が単純な「前日比」ではなく `distance(day n) = meter(day n+1) - meter(day n)` であること、15日目だけ点検欄行(22-29)を飛び越えてブロック2に接続すること、31日目は数式なし(空欄)であることを確認。データモデルの計算ロジックはこれに厳密に合わせた。
- xlsx出力方式の検証(スパイク): ExcelJSでサンプルを無変更でload→writeFileしても150個の結合セル・シート間参照式(表紙!E29/E30)・共有数式(G列)・フォント・フッター文言(帳票管理番号)がすべて保持されることをNode.jsで実機検証済み。データセルの値だけを書き換える運用であれば、テンプレートのスタイル崩れリスクは低いと判断し、ExcelJSを採用(JSZip+XML直接編集の代替案は不要と判断)。
- モック画面(ダミーデータ)を作成しレビュー。フィードバックを受けて以下を反映:
  - 運転月報の「休日〇印」欄(手動マーク)を廃止し、日付セルを自動判定で色分け(日曜・祝日=赤、土曜=青)する方式に変更。祝日判定は外部ライブラリを使わず、固定日+ハッピーマンデー+春分秋分近似式+振替休日を自前実装(`app.js`の`computeJapaneseHolidays`)。
  - デザインをgenka-webappの配色(`--accent: #2f6fed`等)に統一。ナビゲーションはiPhone幅では画面下部固定タブバー、PC幅では上部タブに切り替わるレスポンシブ構成にした(genkaは上部タブ固定だが、本アプリは現場での片手入力が主用途のため意図的に変更)。
  - 運転月報の見出し・ラベルはサンプルの明朝体、データ部はゴシック体という原本のフォント使い分けを再現。印刷CSSは`@page`でA4・サンプルに近い余白を指定。
  - 「1日に複数トリップ」を一時検討したが、サンプルが1日1行の紙運用であることと実装の複雑化を踏まえ、最終的に1日1車両=1レコード(上書き運用)で確定。
- データ層(`storage.js`)を実装し、Node上でvmサンドボックスに読み込んでCRUD・マージロジックをテスト(ブラウザなしで検証、`storage-test.js`は使い捨てスクリプトのため未コミット)。全画面がstorage.js経由でのみデータに触れる設計を徹底。
- 3画面(社有車リスト・運転記録入力・運転月報)を実データに接続。Playwrightで実ブラウザ操作による結合テストを実施し、以下を確認:
  - 車両登録→運転記録2件入力→月報表示で、走行距離(15300-15230=70km)・給油合計・燃費が正しく計算されて表示される。
  - Excel出力ボタンで生成された実ファイルをNode/ExcelJSで再検証し、データセルの値・150個の結合セル・共有数式(G列)・シート間参照式(表紙!E29/E30)・フッター文言がすべて正しく保持されていることを確認。
  - 社有車リストのExcel出力→同じファイルを再取込した際、内容が同一であれば競合として検出されない(差分がある場合のみ競合パネルが出る)ことを確認。
- ExcelJS/SheetJSは重量級(合計1.8MB程度)のため、`app.js`に`loadScriptOnce()`を実装し、実際にその機能を使う画面操作時にのみ動的読み込みする方式にした(iPhoneでの運転記録入力の初期表示を軽く保つため)。
- xlsxテンプレートの実セルレイアウトをExcelJSで直接調査し、当初の想定と異なっていた点を修正: 部店所名の入力欄はA2ではなくE2(A2は固定ラベル)、点検結果の入力セルはO23〜O28(15日ブロック)/O49〜O54(末日ブロック)。
- 日常点検項目のUIをサンプルExcelの実物レイアウトに合わせて全面的に作り直した(縦書きの「点検指定日」ラベル、項目番号+正式な点検内容の説明文、結果列の色分け、下部の記入方法注記)。あわせて`FIXED_CHECKLIST_ITEMS`(`storage.js`)も短縮ラベルからサンプルの正式な項目文言に変更。
- 動作確認中、`index.html`をブラウザキャッシュが古いまま保持し変更が反映されない事象が発生。genka-webappの`style.css?v=...`と同じ命名規則で全アセットにバージョンクエリ(`?v=20260720a`)を付与し、以後CSS/JSを変更したらこの値を更新することでキャッシュ無効化する運用にした(house convention)。
- さらに`app.js`でService Worker登録(`sw.js`)を追加した後、キャッシュファースト方式のため一度インストールされると`?v=`だけでは新しい`index.html`自体が配信されない事象が判明。`sw.js`のCACHE_NAME(バージョン文字列)も合わせて更新することで新しいService Workerへの切り替えを促す必要がある。以後、キャッシュ対象ファイルを変更した際は`index.html`の`?v=`と`sw.js`のCACHE_NAMEを両方更新すること(house convention)。
- 上記の運用は開発中の頻繁な変更のたびにユーザーへ手動Unregisterを依頼することになり負担が大きいと判断し、**開発が一段落するまでService Workerを一時的に無効化**することにした。単に`app.js`の登録処理を消すだけでは、既に古いキャッシュファーストSWが有効化済みの端末では「新しいapp.js自体が古いSWのキャッシュ経由で配信され続けて、無効化コードが実行されない」という堂々巡りになることに気づいた。そのため`sw.js`を「インストールされたら即座に全キャッシュ削除→自分自身をunregister→制御中のページを再読み込みする」キルスイッチ的な内容に置き換えた。ブラウザは`sw.js`本体の内容変更をfetchキャッシュとは独立して(バイト単位で)検知するため、この方式なら通常のリロード1回で自動的に元の状態(SW未登録)に戻ることを実機で確認済み(旧キャッシュファーストSWを一旦登録した状態から、通常のnavigateを1回行うだけで`getRegistrations().length === 0`になることを検証)。キャッシュファースト設計自体はこの節に残してあるので、PWA仕上げの段階で復元する。
- 日常点検項目のtd要素に`text-align:left`を指定しても`.report-table th, .report-table td`(class+要素セレクタ)の方が詳細度が高く上書きされない不具合があった。`.report-table td.checklist-item`のようにth/td込みで詳細度を揃えて解決。同様に、`table-layout:fixed`のテーブルでは1行目のcolspanセルでしか列幅が決まらず2行目以降のtd幅指定が無視される仕様があり、`colgroup`+`col`で列幅を明示する方式に直した(いずれも日常点検項目テーブルで発生)。
- 部店所名を自由入力からプルダウンに変更。選択肢は社内の支払伝票・振替伝票アプリ(`C:\Users\genba-user\OneDrive\MCフォルダ\支払伝票PJ\`)の事業所マスタと同一の7拠点(本店/東関東支店/横浜支店/大阪支店/名古屋支店/仙台支店/北関東支店)を`storage.js`の`OFFICE_NAMES`に定義して使用。
- 15日・月末点検イベントと給油の後日記入を実装。
  - `storage.js`に`checklistEventsDue(record, savedDay)`を追加。「この記録を保存」した日が15日/月末日以降で、かつ該当チェックリスト(`checklistMid`/`checklistEnd`)が未記入(いずれかの`result`が`null`)なら発生対象とする判定にした。これにより、15日・月末そのものに運転が無くても「所定日以降で最初に保存した日」に発生し、記入済みなら再発生しない(`isChecklistComplete`で判定)。
  - `trip-entry.js`の運転記録保存後にこの判定を呼び、該当があれば画面上部に点検記入パネル(○/×選択+保存/後で記入する)を表示するキュー方式にした。「後で記入する」を選んでも記入完了までは次回保存時に再度表示される。
  - 給油の後日記入は、既存の運転記録フォームを流用すると空欄で上書きしてしまう(メーター・行先・運転者が消える)問題があったため、`storage.js`に`saveFuelOnly()`を新設し、給油量以外のフィールドに一切触れない設計にした。UI側は運転記録入力画面に「運転記録入力／給油を後日記入」のモード切替を追加し、給油専用の簡易フォーム(車両・日付・給油量のみ)を用意した。
  - Node上のvmサンドボックステストで、15日未満は発生しない/15日以降に繰り越して発生する/記入済みなら再発生しない/月毎の末日(28日・31日等)を正しく判定/給油のみ記入で他フィールドが保持される、の各ケースを検証済み。
- 「部店所名」を「事業所名」に呼称変更した上で、運転月報画面での自由入力/プルダウン選択をやめ、社有車リスト(`vehicles.js`)の車両マスタに`officeName`フィールドを追加してそこで選択する方式に変更。運転月報側は選択中の車両の`officeName`を都度参照して表示するだけ(読み取り専用)にし、月報レコード自体には事業所名を保持しない設計にした(点検項目文言と同じ「都度参照」パターン)。私有車の場合は転記元の車両マスタが無いため空欄になる。
- 走行距離合計セルの幅を上部日次表の「走行距離」列と揃え(`.distance-cell`共通クラス)、燃費ラベルセルの幅を「行先」列と揃えた(`.fuel-economy-label`)。
- Service Worker(`sw.js`)を開発中は一時的に無効化。頻繁な変更のたびに手動Unregisterが必要で確認の妨げになっていたため、`sw.js`を「インストールされたら即座に全キャッシュ削除→自分自身を登録解除→ページ再読み込み」というキルスイッチ内容に置き換えた。`app.js`側の新規登録処理も削除。ブラウザは`sw.js`本体のバイト変更を独立して検知するため、通常のリロード1回で既存の古いService Workerも自動的に解除されることを実機で確認済み(旧キャッシュファーストSWを登録した状態から通常navigateを1回行うだけで`getRegistrations().length === 0`になることを検証)。元のキャッシュファースト設計は本DEVLOGの記述を元にPWA仕上げの段階で復元する。

- 初回コミットを作成し、GitHubへ公開(genka-webappと同じ方式)。パブリックリポジトリ、`gh api repos/.../pages`でGitHub Pages(Source: GitHub Actions)を有効化し、既存の`.github/workflows/pages.yml`で`public/`が自動デプロイされることを確認済み。
  - リポジトリ: https://github.com/shar19-ops/unten-geppo-webapp
  - 公開URL: https://shar19-ops.github.io/unten-geppo-webapp/
  - デプロイ後、`index.html`・`storage.js`・`assets/unten-geppo-template.xlsx`がいずれも200で配信されることを確認済み。

## 2026-07-21 QRコードによる車両自動選択

- 社有車には「個人専用」(特定社員のみ運転)と「共用」(不特定多数の社員が運転)があり、共用車では運転記録入力のたびに車両を手動選択する手間・選び間違いが課題という相談を受け、ブレインストーミングの結果、車両ごとにQRコードを発行して現物に貼り付け、iPhoneのカメラで読み取るだけで運転記録入力画面にその車両が自動選択された状態で開く、という方式を採用した。個人専用車も同じ仕組みで運用できる。
- QR生成には外部npm依存を追加せず、既存のExcelJS/SheetJSと同じ「vendor配下に単一ファイルの軽量ライブラリを物理配置する」方針を踏襲し、`qrcode-generator@2.0.4`(MIT、Kazuhiko Arase氏作)を`npm pack`で取得して`public/vendor/qrcode/qrcode.js`に配置した。QRは`<現在のorigin+pathname>?vehicle=<車両ID>`をSVGとしてその場で生成し、印刷してA4で車両に貼り付けられるようにした(画像読み込みのレースコンディションが無いインラインSVG方式)。
- QRコードは車両IDをそのまま運ぶだけの「ラベル」とし、新規のデータモデルは追加していない。該当車両が車両リストから削除されればそのQRは単に「該当車両なし」として機能しなくなる、という運用でよいという要望に合わせた。
- `app.js`に起動時処理(`applyQrVehicleParam`)を追加し、`?vehicle=`パラメータを読み取って該当車両を運転記録入力へ引き渡した後、`history.replaceState`でURLをきれいに戻す(同じQRを再度読み取っても同じ処理が繰り返せるように)構成にした。
- 設計書: `docs/superpowers/specs/2026-07-21-qr-vehicle-select-design.md`、計画書: `docs/superpowers/plans/2026-07-21-qr-vehicle-select.md`。

## 2026-07-21 車両リストの社有車/私有車統合(私有車の登録制移行)

- 従来、私有車は運転記録入力のたびに使用者が車両情報を自由入力する方式だったが、これを「私有車使用届(紙の書式)が回送されてきたら管理者が車両リストに事前登録し、QRコードを運転者へ配布する」という社有車と同じ運用に統一したいという要望を受け、設計を全面的にやり直した。
- 画面名を「社有車リスト」から「車両リスト」に変更し、社有車/私有車のタブ切替構成にした(`vehicles.js`)。私有車には「使用者名」(後に「車両管理者」へ統一、後述)という必須項目を追加し、社有車と同じ`vehicleType`フィールド(`'company'`|`'private'`、未設定は後方互換で社有車とみなす)で区別する設計にした。
- 運転記録入力の私有車選択を、自由入力(`privateCarLabel`)から車両リスト登録済み車両の選択式(`vehicleId`)に変更。Excel/JSON入出力・競合解決(`mergeVehicles`)も両区分に対応させ、運転月報の事業所名転記(既存の社有車向け仕組み)も私有車の登録内容から表示されるようにした。
- QRコードによる自動選択も私有車に対応させ、`app.js`側で車両の`vehicleType`に応じて運転記録入力の社有車/私有車モードを自動的に切り替えるようにした。
- 設計書: `docs/superpowers/specs/2026-07-21-vehicle-list-unification-design.md`、計画書: `docs/superpowers/plans/2026-07-21-vehicle-list-unification.md`。

## 2026-07-22 給油UI簡素化・車両リストの管理者パスワード保護

- 運転記録入力は出庫前(運転前)のみの記録であり、給油量はその時点では分からないため、フォーム内の「給油あり」チェックボックス+給油量欄は実際には使われない項目と判断し削除した。あわせて別タブの「給油を後日記入」を「給油入力」に改称。保存処理から`fuelAdded`キーを完全に取り除く(`null`で明示的に上書きするのではなくキー自体を渡さない)ことで、`saveTripDay`の既存のオブジェクトスプレッドマージ挙動により、既に「給油入力」タブで記録済みの給油量が運転記録の再保存で消えないようにした。
- 車両リストは誰でも自由に追加・編集・削除できてしまう状態だったため、ユーザーが別途運用している馬券投票アプリ(`keiba-baren-v3.html`)と同じ方式(パスワードをJSコード内の定数と単純比較、`sessionStorage`で解除状態を保持、ブラウザ/タブを閉じると解除状態は消える、というクライアント側のみの簡易な仕組み。本格的なサーバー側認証ではない)で保護することにした。パスワードは`anzen_kanri`。未解除時は「車両リスト」タブ自体を非表示にし、ヘッダーの「管理者」チェックボックス+パスワード入力オーバーレイで解除する構成にした(`app.js`)。
- 設計書: `docs/superpowers/specs/2026-07-22-fuel-entry-simplification-design.md`、`docs/superpowers/specs/2026-07-22-vehicle-list-admin-password-design.md`。計画書: `docs/superpowers/plans/2026-07-22-fuel-ui-and-admin-lock.md`(2機能を1本にまとめて実施)。

## 2026-07-22 車両マスタのFirebase共有(クラウド同期)

- QRコードを読み取っても「対応する車両が見つかりませんでした」と表示される不具合が発生。原因は、車両マスタが`localStorage`(ブラウザ・端末ごとに独立した保存領域)に保存されており、車両を登録した端末と異なる端末でQRを読み取ると、その端末のローカル保存領域には当該車両の情報が存在しないためだった。これは「共用の社有車を複数の社員のスマホで運用する」という当初の要件とlocalStorage方式が根本的に噛み合っていなかったことによるもので、設計上の見落としだった。
- 対応として、車両マスタ(社有車・私有車リストのみ。運転記録・給油記録・月報は対象外)をFirebase Realtime Databaseに同期する方式に変更した。馬券投票アプリと同じくFirebase SDKは使わず素の`fetch()`のみで読み書きし(ビルド不要という既存方針を維持)、ルールは`{".read":true,".write":true}`の全開放(既存の管理者パスワードと同じ「クライアント側のみの簡易保護」というセキュリティモデルの延長として、同じ前提・同じリスク許容度で採用)。`localStorage`は表示用キャッシュ兼オフライン時のフォールバックという位置づけに変わった。
- アプリ専用の新規Firebaseプロジェクトを作成し(馬券投票アプリとはデータを分離)、`storage.js`に`FIREBASE_DB_URL`定数(`ADMIN_PASSWORD`と同じプレーンな定数埋め込み方式)と`syncVehiclesFromCloud`/`pushVehicleToCloud`/`deleteVehicleFromCloud`/`pushVehiclesToCloud`を追加。起動時・車両リスト画面を開くたびにクラウドから同期し、通信エラー時は読み込みならローカルキャッシュへ静かにフォールバック、書き込みならエラー表示のうえキャッシュを変更しない(食い違い防止)設計にした。
- レビューで見つかった2件の不具合(通信エラー時に車両フォーム・競合解決パネルの入力内容が消えてしまう)を修正。また、Firebaseルールを全開放にしたことで「DB URLを知っていれば誰でも車両データを書き換えられる」状態になった結果、既存の(エスケープなしで描画されていた)車種名等の表示箇所が、自分のブラウザだけでなく全利用者に影響しうるクロスユーザーの書き込み型XSSのリスクに変わったため、`storage.js`に`escapeHtml`ヘルパーを追加し、車両データを描画する全箇所(車両リスト・運転記録入力・運転月報)にエスケープ処理を適用した。実際に`<script>`/`<img onerror=...>`ペイロードをFirebaseへ直接書き込んで、エスケープされ実行されないことを実機検証済み。
- 検証はすべて、本番運用で実際に登録された車両データ(Firebase上に実在)を一切変更・削除せず、読み取り専用またはテスト車両の個別追加・削除で行った。
- 設計書: `docs/superpowers/specs/2026-07-22-vehicle-master-cloud-sync-design.md`、計画書: `docs/superpowers/plans/2026-07-22-vehicle-master-cloud-sync.md`。

## 2026-07-22 QRコード経由アクセス時の車両ロック

- QRコード経由で車両が自動選択されても、運転記録入力・運転月報の車両選択欄自体は従来通り操作可能なままで、誤って別の車両を選択・記録・閲覧できてしまう状態だったため、QRで車両が正常に特定できた場合は運転記録入力・運転月報の両画面で車両選択欄を非表示にし、固定表示に置き換える「ロック」機能を追加した。管理者パスワード解除済みでも例外なくロックされ、解除用のUIはあえて設けていない(別の車両を扱う場合はQRパラメータなしでサイトを開き直す)。QRで指定された車両が見つからない場合(削除済み等)は、ロックをかけず通常の選択式にフォールバックする。
- 実装中、運転月報側のロック判定に不具合を発見・修正した。実在しない車両IDでロックを試みた際、`reportVehicleOptions()`側は正しく全車両リストへフォールバックしていたにもかかわらず、画面側のロック判定が「車両IDが設定されているかどうか」だけを見ていたために誤って固定表示のままになってしまう(=本来出るべき通常の選択式に戻らない)というもの。「フィルタ後の選択肢が1件かつ、その1件が実際にロック対象のIDと一致するか」まで確認する`isLocked`判定に直して解消した。
- 設計書: `docs/superpowers/specs/2026-07-22-qr-vehicle-lock-design.md`、計画書: `docs/superpowers/plans/2026-07-22-qr-vehicle-lock.md`。

## 2026-07-22 車両管理者の表記統一・アプリアイコン設定

- 車両リストの「使用者名」(私有車)・「既定の車両管理者」(社有車)という別々の名称・データ項目を、実質的に同じ概念として「車両管理者」に統一。内部データも`defaultManager`/`driverName`という2つのフィールドを廃止し`vehicleManager`という1つのフィールドに統一したが、Firebase上に既に存在する実データ(旧`defaultManager`のみ保存済みの社有車)については一括移行を行わず、読み取り時に`v.vehicleManager ?? v.defaultManager ?? v.driverName ?? ''`という優先順でフォールバックする方式にした(該当車両を次回編集・保存すれば自然と新フィールドのみで保存され直る)。
- あわせて、運転月報の「車両管理者」欄を、従来の「手入力して月報レコードに保存」する方式から、既存の「事業所名」欄と全く同じ「車両リストの登録内容から毎回読み取り専用で転記する」方式に変更した。私有車も社有車と同様に転記されるようになり、従来の「私有車は常に空欄」という制限が無くなった。Excel出力の書き込み先セルも、月報レコードの値ではなく画面側で計算した転記値を渡すように`xlsx-export.js`の関数シグネチャを変更した。
- アプリのアイコンが未設定(ブラウザタブは`favicon.ico`の404が開発中ずっと出ていた状態、PWAマニフェスト・iOSホーム画面追加用も仮のプレースホルダー画像のまま)だったため、ユーザーが用意したロゴ画像(`運転月報.png`、392×392px)から、追加のnpm依存を導入せずWindows標準の.NET(`System.Drawing`)をPowerShell経由でリサイズし、192×192/512×512/180×180の3サイズを生成して`favicon`・PWAマニフェスト・`apple-touch-icon`に反映した。これにより開発中ずっと出ていた404エラーも解消された。
- 設計書: `docs/superpowers/specs/2026-07-22-vehicle-manager-unification-design.md`、`docs/superpowers/specs/2026-07-22-app-icon-design.md`。計画書: `docs/superpowers/plans/2026-07-22-vehicle-manager-and-app-icon.md`(2機能を1本にまとめて実施)。

## 2026-07-22 運転月報の走行距離計算のギャップ対応

- 運転月報の走行距離(`computeDistance`)は、サンプルExcelのG列数式ロジックを再現し「対象日の翌日の出庫時メーター指針との差分」で計算していたが、休日等で翌日にメーター指針が未入力の場合、対象日の走行距離が空欄のままになってしまうという指摘を受けた。翌日固定ではなく、対象日以降で月末(31日)までの間に次にメーター指針が記録されている日を探し、その日までの差分を取る(間の空欄日は飛び越える)方式に変更した。
- 合計走行距離の計算(`computeTotals`)自体は変更していない。日ごとの差分がテレスコーピング(打ち消し合い)する性質上、ギャップを飛び越えても二重計上・欠落は発生しない。実機検証でも、偶然残っていた10日以上に渡る大きなギャップを含むデータで合計値が正しく一致することを確認できた。
- Excel出力(テンプレートのG列数式)は今回のスコープ外としたため、休日等を挟む月は画面表示とExcel出力の走行距離が食い違いうる状態が残っている(画面表示が正しい値)。将来的にExcelテンプレート側の数式も対応させるかは今後の検討課題。
- 設計書: `docs/superpowers/specs/2026-07-22-distance-gap-fix-design.md`、計画書: `docs/superpowers/plans/2026-07-22-distance-gap-fix.md`。

## 2026-07-27 発行者確認Teams通知の実機不具合調査・修正

- 月末点検完了時にMicrosoft Teamsへ自動通知する機能(既存)について、実際には一度もTeamsチャンネルに通知が届いていなかったとの連絡を受け調査。原因は2点重なっていた: (1) Power Automate側のアクションが「カードを投稿する」(`PostCardToConversation`)になっており、Adaptive Card形式のJSONを要求するのに対しアプリ側は普通のテキスト(`{"text": "..."}`)しか送っていなかったため、検証エラーで毎回失敗していた(HTTP 202はWebhookトリガー自体の受信成功を示すだけで、フロー内部の投稿処理が落ちていたことまでは分からなかった)。(2) 投稿先のrecipient IDが不完全な値になっていた。
- 使い捨てテスト通知(冒頭に「テスト通知」と明記)を実際のWebhookへ複数回送信して原因を切り分け、Power Automate側のアクションを「チャットまたはチャネルにメッセージを投稿する」に差し替え、投稿先を選び直すことで解消。修正後、実際にTeamsチャンネルへメッセージが表示されることを確認済み。
- 検証中、誤って実車両(フェラーリ・カローラ)の本番データ(運転記録・点検結果)へ書き込んでしまう事故が2件発生した。いずれも直後に判明し、このセッション内で確認していた直前の実データの値を元に復元済み(本番データは非公開のため書き込み自体は許容されている旨、後日ユーザーより確認済み)。

## 2026-07-27 車両リスト・運転月報の機能整理(JSON入出力廃止・QR URLコピー・印刷/Excel出力の管理者限定化)

- 使われていない車両リスト・運転月報のJSON取込/出力ボタンと関連の競合解決UIを削除(Excel取込/出力は維持)。JSON専用だった`storage.js`のヘルパー(`exportMonthlyLogToFile`/`exportVehiclesToFile`/`readJsonFile`/`mergeMonthlyLog`)も未使用になったため削除。
- 車両リストのQRコードパネルに「URLをコピー」ボタンを追加(クリップボードへコピー)。
- 運転月報の「印刷/PDF」「Excelとして出力」ボタンを、既存の管理者パスワード機構(`isAdminUnlocked()`)で管理者限定にし、非管理者は内容のプレビューのみ閲覧可能にした。管理者トグルのON/OFF時、運転月報タブを表示中であればその場でボタン表示を再描画するようにした。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/12

## 2026-07-28 運転月報4点修正(Excel出力ボタン削除・印刷ボタン色変更・月末走行距離自動計算・アルコールチェック警告・点検×時の案内)

- 管理者モードの「Excelとして出力」ボタンを削除(ボタン・イベント登録のみ。`exportMonthlyLogToXlsx`本体・`xlsx-export.js`自体は将来の復活に備えて残置)。「印刷/PDF」ボタンをプライマリ(青)配色に変更。
- 月末日の走行距離が、同月内に次の記録が無いために空欄のままだった問題を解消。`computeDistance`/`computeTotals`(`app.js`)に`year`/`month`/`nextMonthDays`を渡せるよう拡張し、月の最終日だけ翌月最初の記録から計算するようにした(月末以外の日には影響しない設計)。
- 運転記録入力でアルコールチェック値が0.15mg/L以上のとき警告ポップアップ(`alert()`)を表示。
- 15日・月末点検で1項目でも×があれば、社有車/私有車で文言を分けた案内ポップアップ(社有車:自動車修理依頼書、私有車:整備工場)を表示。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/13

## 2026-07-28 アルコールチェック2回化・Teams通知の一時停止・発行者確認バナーのタイミング変更

- アルコールチェックを単一値から始業前(`alcoholCheckBefore`)・終業後(`alcoholCheckAfter`)の2フィールドに分割。既存データの引き継ぎは行わず、今後の入力分から新フィールド構成で運用する方針とした。
- Teams通知を`TEAMS_NOTIFICATIONS_ENABLED`フラグで一時停止。Webhook設定・呼び出し箇所は変更せず、再開時はフラグを戻すだけで済むようにした。
- 発行者確認バナーの表示条件を「今月分の月末点検完了後」から「翌月の運転記録が1件でも保存された後」に変更(月末点検の完了状況は見ない)。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/14

## 2026-07-28 運転月報の提出ボタン文言変更・アルコール未入力バリデーション・表の直接編集・桁区切り

- 発行者確認バナーの文言・ボタン名を「確認しました」から「提出」に変更。
- アルコールチェック(始業前/終業後)が片方だけ入力されている日があると、「提出」時にエラー表示+該当セルをピンクでハイライトするバリデーションを追加(`findAlcoholErrorCells`)。
- 運転月報の表(メーター指針・行先・運転者・アルコール2項目・給油量)をセル単位で直接編集・自動保存できるように変更(`<td>`を`<input>`化し、`.report-sheet`への`change`イベント委譲で`saveTripDay`/`syncLogDayToCloud`を呼ぶ)。走行距離は自動計算のため引き続き編集不可。
- 走行距離合計に`toLocaleString()`で桁区切りを追加。
- 検証中に発見した不具合: バリデーションエラー表示後に不足値を修正して再提出すると成功するにもかかわらず、直前のエラーメッセージ文言が消えずに残ってしまう問題があり、成功時に`reportStatusMessage`をクリアするよう修正。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/15

## 2026-07-28 管理者パスワード変更

- 管理者パスワードを`anzen_kanri`から`anzn`に変更。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/16

## 2026-07-28 月末走行距離自動計算のクロスデバイス不具合修正

- 月末走行距離の自動計算(2026-07-28実装分)が、翌月データが別端末でしか入力されていない場合に空欄のままになる不具合が見つかった。原因は、当月分の月報はクラウド(Firebase)と同期する一方、計算に使う翌月分の月報は`loadMonthlyLog()`でローカルストレージのみを参照しており、クラウド同期をしていなかったため。
- 当月分と同じ`syncMonthlyLogFromCloud`を翌月分にも適用し(`reportNextMonthSyncedKey`で同期済みキーを管理)、`app.js`の`showView('report')`でもこのキーをリセットするようにした。
- 検証は、使い捨てテスト車両に対し、当月分をこのブラウザから、翌月分をこのブラウザのlocalStorageを経由せずFirebaseへ直接書き込む(「別端末での入力」を模擬)ことで、修正前は空欄になるはずの計算が正しく行われることを確認した。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/17

## 2026-07-28 安全運転管理者の確認フロー(承認・差戻し)を追加

- 車両管理者の「提出」後、安全運転管理者による確認フローを追加。管理者モードで提出済み・未確認の運転月報を開くと確認・差戻しパネルが表示され、氏名を入力して「確認」を押すと安全運転管理者欄に日付+入力した氏名(そのまま表示、発行者欄のような姓の自動抽出はしない)が記入される。
- 「差戻し」を押すと、件名「運転月報の再提出依頼」・本文を自動入力した`mailto:`リンクを開き、車両管理者側の提出済み状態(発行者欄・`issuerConfirmedAt`)を解除して再提出可能な状態に戻す。宛先(To)はマスタにメールアドレスが無いため空欄のまま(手動入力してもらう方針)。
- 検証中に判明した重要な仕様: Firebase Realtime Databaseは値が`null`のキーをPUT時に削除してしまうため、「発行者確認済み→差戻しで未確定に戻す」という今回初めて発生するリセット操作が他端末に正しく伝わらない(古い確定状態が残ってしまう)問題があった。未確定状態の初期値を`null`から空文字列に変更し(Firebaseは空文字列を削除しないため)、クラウド同期のマージ処理(`syncMonthlyLogFromCloud`)も「クラウド側が最新と判定済みの場合は明示的な値をそのまま採用する」方式に修正して解消した。この教訓は`issuerConfirmedAt`に限らず、今後「一度確定した値をnullへ戻す」操作を追加する際は同様の対応が必要になる点に注意。
- 差戻しメール本文は当初、依頼文に句読点が無いまま実装してしまい、後から読点の追加と行頭の余計な全角スペースインデントの削除を行った。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/18

## 2026-07-28 車両リストの並び順固定(事業所順)

- クラウド同期のたびにFirebaseから返る車両データの順序が変わり、車両リストの表示順が意図せず入れ替わってしまう不具合が見つかった。`vehicles.js`に`officeSortIndex`/`sortVehiclesByOffice`を追加し、`OFFICE_NAMES`(本店→東関東支店→横浜支店→大阪支店→名古屋支店→仙台支店→北関東支店)の固定順→車両番号順で常に並べ直すようにした。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/20

## 2026-07-29 完了トースト表示・QR固定の更新後継続・QRコード再発行機能

- 保存・提出・確認・差戻し等の主要操作が完了した際、数秒で自動的に消えるトースト通知(`showToast()`)を表示するようにした。運転月報の表の直接編集による自動保存は対象外(頻発して煩わしくなるため)。
- QRコードで選択された車両の「固定」が、ブラウザ更新のたびに解除されてしまう不具合を修正。`sessionStorage`(`ug_qr_vehicle_id`)にタブ単位で車両IDを記憶し、更新後も同じ車両に固定され続けるようにした。管理者モードでは常にロック自体がかからないようにも徹底(`!isAdminUnlocked()`を各所に追加)。
- QRコード紛失時のために再発行機能を追加。既存の`?vehicle=<車両ID>`直付け方式は維持しつつ、新規発行・再発行分は`?qrToken=<トークン>`方式(再発行すると新しいトークンに入れ替わり旧トークンは無効化)に切り替えた。
- 検証中に2件の不具合を発見・修正: (1)管理者トグルのON/OFF時に運転記録入力タブが再描画されずロック解除が反映されない、(2)無効化された旧`qrToken`でアクセスした際、`sessionStorage`の古いロックへ誤ってフォールバックしてしまい「該当車両なし」の警告が出ずに別の車両のロックが復活してしまう問題。`bootstrapApp()`のQR解決ロジックを、URLに`?vehicle=`/`?qrToken=`が明示的に付いている場合は`sessionStorage`を一切参照しない(無効なトークンをこっそり救済しない)ように再構成して解消した。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/21

## 2026-07-29 アプリタイトルにバージョン表示を追加

- ヘッダーの「運転管理月報」の隣に、既存のキャッシュバスティング用`?v=`と同じ値を控えめなグレー文字で表示するようにした(`index.html`の`#appVersion`)。今後のPRごとにこの値も手動で合わせて更新していく運用とし、実際にブラウザへ反映されているバージョンを画面上で確認できるようにした。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/22

## 2026-07-30 運転月報:管理者向け「提出状況一覧」

- 車両を1台ずつ選んで運転月報を開かないと提出状況が分からなかったため、管理者モード限定で「運転月報」タブ内に切替ボタンを追加し、選択した年月について全車両分の提出状況(未提出/提出済み・承認待ち/承認済み)を事業所順で一覧表示できるようにした(`renderReportStatusList`)。行をクリックするとその車両・月の通常の運転月報表示にすぐ切り替わる。
- 全車両・全月分の運転記録本体を毎回取得すると無駄が大きいため、Firebaseの`/logs.json`を1回だけ読み、対象年月のメタ情報(提出・承認状況)だけを抽出する`fetchSubmissionStatusForMonth()`を新設した。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/23

## 2026-07-30 車両選択プルダウンの並び順統一

- 車両リスト画面には事業所順ソート(2026-07-28実装)が適用済みだったが、運転記録入力・給油入力・運転月報の車両選択プルダウンには適用されておらず、`loadVehicles()`の生の配列順(ソートなし)のままだった不整合が見つかった。同じ`sortVehiclesByOffice()`を適用して並びを統一した。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/24

## 2026-07-30 Excel取込の見出し行自動検出・私有車の使用許可期限管理

- 実際の車両台帳Excelファイル(社内で運用中のもの)を使ってExcel一括取込機能の適合性を検証した際、シートの1行目を見出し行と決め打ちしている現行実装では、タイトル行が上に数行あるレイアウト(実ファイルは6行目が本当の見出し)だと「車両番号」列が見つからず取込エラーになることが判明。「車両番号」列を含む行を自動検出して読み込むように修正した(`findVehicleHeaderRowIndex`)。実データそのものの取込は今回スコープ外とした。
  - 併せて、既存のマージロジック(`mergeVehicles`)が「事業所種別+車両番号」で既存車両と突き合わせ、内容が完全一致すれば`id`/`qrToken`(QRコードのURL)に一切触れず、内容が違う場合も競合解決で更新されるのは`nickname`/`officeName`/`active`/`vehicleManager`のみで`id`/`qrToken`は変更されない設計であることを確認済み(取込によって既存車両のQR URLが変わることはない)。
- 私有車に「使用許可期限」欄を追加。期限を過ぎると運転記録入力・給油入力の車両選択から自動的に外れ(`isVehicleUsable`/`isPermitExpired`、`storage.js`)、車両リストには「期限切れ(使用不可)」と表示される。期限を延長(新しい日付を保存)するだけで元通り選択できるようになり、QRコードのURLは変更されない(既存の部分更新の仕組みをそのまま利用)。
- 使用許可期限が1週間以内に迫っている私有車がある場合、アプリ起動時に管理者モードに関係なく全ユーザーへ警告ポップアップを1度だけ表示する(端末の`localStorage`に車両ID+期限日付をキーに記録。期限を延長すると新しい日付として再度警告できる)。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/25

## 2026-07-30 期限切れ・停止中の車両のQRコードで他の車両を選べてしまう不具合を修正

- 前日実装した使用許可期限管理(PR #25)のQR連携部分に不具合が見つかった。使用許可期限切れ(または停止中)の車両のQRコードを読み込むと、その車両が「見つかりませんでした」という扱いになり、運転記録入力画面が通常の開放的な車両選択(社有車/私有車を自由に選べる状態)に落ちてしまっていた。本来はその特定の車両だけが使えないはずが、結果的に別の車両を自由に選んで入力できてしまう状態になっていた。
- `app.js`の`bootstrapApp()`で、QR対象の車両IDが「そもそも存在しない(削除済み・無効なトークン)」場合と「存在するが使用不可(期限切れ・停止中)」場合を区別するように修正。後者は理由を明示した案内文(`buildQrBlockedMessage`)だけを表示し、`trip-entry.js`側で運転記録入力・給油入力のフォーム自体を出さないようにした(`tripQrBlockedMessage`)。管理者モードのみ、これまで通り通常の車両選択画面に進める例外とした。
- ブロック状態は`sessionStorage`(`ug_qr_blocked_vehicle_id`)に保持してページ更新後も維持されるが、読み込みのたびに車両の最新状態を再確認するため、管理者が使用許可期限を延長すれば次の読み込みで自動的に解消される。
- PR: https://github.com/shar19-ops/unten-geppo-webapp/pull/27

## 今後の改修候補(未着手・メモ)

- **給油登録の車両選択見直し**(保留中): 運転記録入力と給油入力とで車両選択の仕組みを統一する改修案が session 中に一度提起されたが、他の改修を優先するため保留にしたまま。給油UI簡素化(2026-07-22)はこの保留案とは別の対応であり、この保留案自体はまだ未着手。
- **Excel出力の走行距離ギャップ対応**(2026-07-22発生): 画面表示側は休日等のギャップに対応したが、Excel出力(テンプレートのG列数式)は対象外のまま。テンプレートの数式自体を修正する必要があり、影響範囲の見極めが必要。
- **複数管理者による車両リストの同時Excel/JSON取込**(2026-07-22、設計時に指摘・受容): 一括取込確定時にFirebaseへ全体を上書きする仕様のため、管理者が同時に作業すると片方の変更が消えうる。現状の運用(管理者は基本1人)では実害が低いと判断し、対応不要と結論済み。
- ~~**運転記録入力の初期設定**(2026-07-20、ユーザー依頼)~~: QRコードによる車両自動選択(2026-07-21)・車両リストの登録制移行により、「都度の自由入力」という課題そのものが解消されたため、この改修候補は解決済みとする。
- **Excel出力のアルコールチェック2列化未対応**(2026-07-28発生): アルコールチェックを始業前/終業後の2項目に分割したが、Excelテンプレート(`assets/unten-geppo-template.xlsx`)の列は1つのみのため、始業前分しか出力されない。テンプレート自体の改修が必要(現状「Excelとして出力」ボタン自体が削除済みのため実害は無いが、ボタンを復活させる際は要対応)。
- **実際の車両台帳と車両リストの差分**(2026-07-30、実ファイル突き合わせ時に判明): 社内の車両台帳Excel(`般ー45.車両台帳(2026.6)`)と現在の車両リストを突き合わせたところ、社有車23台は完全一致していたが、私有車2台(千葉341ほ526 トヨタカローラクロス・本店、千葉333の6600 トヨタランクル70・東関東支店)が車両リストに未登録だった。逆に車両リストには台帳に無い車両(旗の台3AAん0000・ロールスロイス・本店)が1台あり、テスト/悪戯データか台帳側の記載漏れかは未確認。件数が少ないため一括取込ではなく手入力での反映を想定しているが、対応自体はまだ未着手。
- **差戻しメールの宛先自動入力**(2026-07-28発生): 車両管理者のメールアドレスがマスタに無いため、差戻しメールは宛先(To)が空欄のまま開く。将来的に車両マスタへメールアドレス項目を追加すれば自動入力できる。
- **副安全運転管理者のデジタル化**(2026-07-23設計時よりスコープ外を継続): 発行者・安全運転管理者は電子的に確認できるようになったが、副安全運転管理者欄は引き続き印刷後の手作業の押印のみ。
